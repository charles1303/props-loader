 Assumptions include the following:
1. It is assumed that the file protocols are restricted only to the file:, http:, and classpath: protocols
2. Only files with extensions .properties and .json are processed
3. The loading of the properties will be loaded only once via a Service Starter or Singleton or Dependency Injection pattern
4. Scalability was considered at loading one hundred(100) properties
